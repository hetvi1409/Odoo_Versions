<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="survey_start_button" name="Survey: start form content"
              inherit_id="survey.survey_fill_form_start">
        <xpath expr="//div" position="replace">
            <div class="wrap o_survey_start">
                <div class='mb32'>
                    <div t-field='survey.description'
                         class="oe_no_empty pb-5 text-break"/>
                    <t t-if="answer.is_session_answer">
                        <div class="fw-bold">
                            The session will begin automatically when the host
                            starts.
                        </div>
                    </t>
                    <t t-else="">
                        <div t-if="survey.is_time_limited">
                            <p>
                                <span t-if="not survey.certification">Time limit
                                    for this survey:
                                </span>
                                <span t-else="">Time limit for this
                                    certification:
                                </span>
                                <span class="fw-bold text-danger"
                                      t-field="survey.time_limit"
                                      t-options="{'widget': 'duration', 'unit': 'minute'}"></span>
                            </p>
                        </div>
                        <button type="submit" value="start"
                                class="btn btn-primary btn-lg disabled">
                            <t t-if="survey.certification">
                                Start Certification
                            </t>
                            <t t-else="">
                                Additional Questions
                            </t>
                        </button>
                        <span class="o_survey_enter fw-bold text-muted ms-2 d-none d-md-inline">
                            or press Enter
                        </span>
                    </t>
                </div>
            </div>
        </xpath>
    </template>

    <template id="survey_fill_for_done_template" name="Survey: retake button"
              inherit_id="survey.survey_fill_form_done">
        <xpath expr="//div[hasclass('wrap')]" position="replace">
            <div class="wrap">
                <div class="o_survey_finished mt32 mb32">
                    <h1 class="fs-2">Thank you!</h1>
                    <div class="row">
                        <div class="col">
                            <div class="col-12 s_website_form_submit mb64"
                                 data-name="Submit Button">
                                <div style="width: 200px"
                                     class="s_website_form_label"/>
                                <a href="/job-thank-you" role="button"
                                   class="btn btn-primary btn-lg s_website_form_send"
                                   id="apply-btn">Submit
                                </a>
                                <span id="s_website_form_result"></span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
</odoo>
