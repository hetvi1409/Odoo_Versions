
<odoo>
  <record id="view_assessment_tree" model="ir.ui.view">
    <field name="name">scs.assessment.tree</field>
    <field name="model">scs.assessment</field>
    <field name="arch" type="xml">
      <tree>
        <field name="name"/>
        <field name="employee_id"/>
        <field name="role_id"/>
        <field name="state"/>
      </tree>
    </field>
  </record>

  <record id="view_assessment_form" model="ir.ui.view">
    <field name="name">scs.assessment.form</field>
    <field name="model">scs.assessment</field>
    <field name="arch" type="xml">
      <form string="SCM Assessment">
        <header>
          <button name="action_submit" type="object" string="Submit" states="draft" class="oe_highlight"/>
          <button name="action_validate" type="object" string="Validate" states="submitted"/>
          <button name="action_done" type="object" string="Mark Done" states="validated"/>
          <field name="state" widget="statusbar"/>
        </header>
        <sheet>
          <style>
              .o_form_view:not(.o_field_highlight)
              .o_field_widget:not(.o_field_invalid):not(.o_field_highlight)
              .o_input:not(:hover):not(:focus) {
              --o-input-border-color: black !important;
              }
              .o_form_view:not(o_field_highlight)
              .o_field_html .note-editable:not(:hover), .o_form_view:not(o_field_highlight) .o_field_html
              .note-editable:not(:focus) {
              border-color: black !important;
              }
              .form-check:not(:hover),
              .form-check:not(:hover) .form-check-input:not(:disabled),
              .form-select:not(:hover), .form-select:not(:hover) .form-check-input:not(:disabled) {
              border-color: black !important;
              }
          </style>
          <div class="oe_title pe-0">
            <h1 class="d-flex flex-row justify-content-between">
              <field name="name" placeholder="Assessment Name"/>
            </h1>
          </div>
          <group>
            <group>
              <field name="employee_id" readonly="1"/>
              <field name="role_id" readonly="1"/>
            </group>
            <group>
              <group>
                <!-- <field name="met_count" readonly="1"/> -->
              </group>
            </group>
          </group>
          <field name="line_ids">
            <tree editable="bottom">
              <field name="competency_id"/>
              <field name="required_level"/>
              <field name="self_level"/>
              <field name="manager_level"/>
              <field name="final_level"/>
              <field name="gap"/>
              <field name="gap_delta"/>
              <field name="evidence"/>
              <field name="comment"/>
            </tree>
          </field>
        </sheet>
        <div class="oe_chatter">
            <field name="message_follower_ids"/>
            <field name="activity_ids"/>
            <field name="message_ids"/>
        </div>
      </form>
    </field>
  </record>

</odoo>
