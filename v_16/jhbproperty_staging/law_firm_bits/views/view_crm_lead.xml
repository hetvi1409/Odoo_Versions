<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_law_crm_lead_form" model="ir.ui.view">
            <field name="name">crm.lead.form.view</field>
            <field name="model">crm.lead</field>
            <field name="inherit_id" ref="crm.crm_lead_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//sheet/div[@name='button_box']/button[@name='action_schedule_meeting']"
                       position="before">

                    <button name="action_open_case" type="object" class="oe_stat_button" icon="fa-legal"
                            context="{'default_crm_lead_id': active_id}">
                        <field name="count_case" widget="statinfo" string="Case"/>
                    </button>

                    <button name="action_open_matter" type="object" class="oe_stat_button" icon="fa-paperclip"
                            context="{'default_crm_lead_id': active_id}">
                        <field name="count_matter" widget="statinfo" string="Matter"/>
                    </button>

                </xpath>
            </field>
        </record>

    </data>
</odoo>