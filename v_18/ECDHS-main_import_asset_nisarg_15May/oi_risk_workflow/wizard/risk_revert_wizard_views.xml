<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="risk_revert_view_form" model="ir.ui.view">
        <field name="name">risk.revert.view.form</field>
        <field name="model">risk.revert.wizard</field>
        <field name="arch" type="xml">
            <form create="0" duplicate="0">
                <header>
                </header>
                <sheet>
                    <group>
                        <field name="risk_type" invisible="1"/>
                        <field name="oi_risk_management_risk" invisible="risk_type == 'activity'"/>
                        <field name="oi_risk_management_activity" invisible="risk_type != 'activity'"/>
                    </group>
                    <group>
                        <field name="comments"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_submit_revert"
                            string="Create Revert"
                            type="object"
                            class="btn-primary"
                            data-hotkey="t"/>
                    <button string="Cancel"
                            class="btn-secondary"
                            special="cancel"
                            data-hotkey="z"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>