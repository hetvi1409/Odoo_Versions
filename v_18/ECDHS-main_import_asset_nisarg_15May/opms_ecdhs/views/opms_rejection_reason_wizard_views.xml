<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_opms_rejection_reason_wizard_form" model="ir.ui.view">
            <field name="name">opms.rejection.reason.wizard.form</field>
            <field name="model">opms.rejection.reason.wizard</field>
            <field name="arch" type="xml">
                <form string="Reject Submission">
                    <div class="alert alert-warning mb-3" role="alert">
                        <strong>Warning:</strong> This will permanently mark the submission as
                        <strong>Rejected</strong>. The reason will be stored on the record and
                        recorded in the chatter audit trail.
                    </div>
                    <group>
                        <field name="submission_id" readonly="1"
                               options="{'no_create': True, 'no_quick_create': True, 'no_create_edit': True}"/>
                    </group>
                    <group string="Reason for Rejection">
                        <field name="rejection_reason" nolabel="1" widget="text"
                               placeholder="Provide a clear reason for rejecting this submission…"
                               required="1"/>
                    </group>
                    <footer>
                        <button name="action_confirm_rejection" type="object"
                                string="Confirm Rejection" class="btn btn-danger"/>
                        <button special="cancel" string="Cancel" class="btn btn-secondary"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_opms_rejection_reason_wizard" model="ir.actions.act_window">
            <field name="name">Reject Submission</field>
            <field name="res_model">opms.rejection.reason.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
