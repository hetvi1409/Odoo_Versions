<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ecdhs_contract_template_view_search" model="ir.ui.view">
        <field name="name">ecdhs.contract.template.search</field>
        <field name="model">ecdhs.contract.template</field>
        <field name="arch" type="xml">
            <search string="Search Templates">
                <field name="name"/>
                <filter string="Active" name="active" domain="[('active','=',True)]"/>
                <filter string="Archived" name="archived" domain="[('active','=',False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Active" name="group_active" domain="[]" context="{'group_by':'active'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="ecdhs_contract_template_view_list" model="ir.ui.view">
        <field name="name">ecdhs.contract.template.list</field>
        <field name="model">ecdhs.contract.template</field>
        <field name="arch" type="xml">
            <list string="Contract Templates" decoration-muted="not active">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
                <field name="is_addendum" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="ecdhs_contract_template_view_form" model="ir.ui.view">
        <field name="name">ecdhs.contract.template.form</field>
        <field name="model">ecdhs.contract.template</field>
        <field name="arch" type="xml">
            <form string="Contract Template">
                <sheet>
                    <style>
                        .o_form_view:not(.o_field_highlight)
                        .o_field_widget:not(.o_field_invalid):not(.o_field_highlight)
                        .o_input:not(:hover):not(:focus) {
                            --o-input-border-color: black !important;
                        }
                        .o_form_view:not(o_field_highlight)
                        .o_field_html .note-editable:not(:hover), .o_form_view:not(o_field_highlight) .o_field_html
                        .note-editable:not(:focus) {
                            border-color: black !important;
                        }
                        .form-check:not(:hover),
                        .form-check:not(:hover) .form-check-input:not(:disabled),
                        .form-select:not(:hover), .form-select:not(:hover) .form-check-input:not(:disabled) {
                            border-color: black !important;
                        }
                    </style>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="sequence"/>
                            <field name="is_addendum" widget="boolean_toggle"/>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                    </group>
                    <group string="Template Body">
                        <field name="body_html" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_ecdhs_contract_template" model="ir.actions.act_window">
        <field name="name">Contract Templates</field>
        <field name="res_model">ecdhs.contract.template</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_active': 1}</field>
        <field name="search_view_id" ref="ecdhs_contract_template_view_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No contract templates found</p>
            <p>Create reusable HTML template blocks for contract drafting.</p>
        </field>
    </record>

</odoo>
