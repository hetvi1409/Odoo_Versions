<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_sagovtender_specification_list" model="ir.ui.view">
        <field name="name">sagovtender.specification.list</field>
        <field name="model">sagovtender.specification</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="requisition_id" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                <field name="department_id" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="view_sagovtender_specification_form" model="ir.ui.view">
        <field name="name">sagovtender.specification.form</field>
        <field name="model">sagovtender.specification</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_submit_for_review" string="Submit for Review" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" string="Approve" type="object" class="btn-success" invisible="state != 'review'"/>
                    <button name="action_reject" string="Reject" type="object" invisible="state not in ['review']"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <style>
                        .o_form_view:not(.o_field_highlight)
                        .o_field_widget:not(.o_field_invalid):not(.o_field_highlight)
                        .o_input:not(:hover):not(:focus) {
                        --o-input-border-color: black !important;
                        }
                        .o_form_view:not(o_field_highlight)
                        .o_field_html .note-editable:not(:hover), .o_form_view:not(o_field_highlight) .o_field_html
                        .note-editable:not(:focus) {
                        border-color: black !important;
                        }
                        .form-check:not(:hover),
                        .form-check:not(:hover) .form-check-input:not(:disabled),
                        .form-select:not(:hover), .form-select:not(:hover) .form-check-input:not(:disabled) {
                        border-color: black !important;
                        }
                    </style>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="description" widget="html"/>
                            <field name="link_to_memo"/>
                            <field name="memo_id" required="link_to_memo" invisible="not link_to_memo" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                            <field name="document_ids" widget="many2many_binary"/>
                        </group>
                        <group>
                            <field name="requisition_id" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                            <field name="department_id" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                            <field name="procurement_method_config_id" readonly="1" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                            <field name="preference_point_system_config_id" readonly="1" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Popup/Dialog View for creating specification from requisition -->
    <record id="view_sagovtender_specification_dialog_form" model="ir.ui.view">
        <field name="name">sagovtender.specification.dialog.form</field>
        <field name="model">sagovtender.specification</field>
        <field name="priority">20</field>
        <field name="arch" type="xml">
            <form string="Specification" create="0" edit="1" delete="0">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="requisition_id" readonly="1" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                        <field name="department_id" readonly="1" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                        <field name="link_to_memo" widget="boolean_toggle"/>
                        <field name="memo_id" invisible="not link_to_memo" options="{'no_create': True,'no_quick_create': True,'no_create_edit': True}"/>
                        <field name="state" invisible="1"/>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description" widget="html" options="{'style-inline': true}"/>
                        </page>
                    </notebook>
                </sheet>
                <footer>
                    <button string="Save &amp; Close" special="save" class="btn-primary"/>
                    <button string="Discard" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_sagovtender_specification" model="ir.actions.act_window">
        <field name="name">Specifications</field>
        <field name="res_model">sagovtender.specification</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>