<?xml version="1.0" encoding="UTF-8" ?>

<odoo>
    <!-- <record id="view_res_config_settings_assets" model="ir.ui.view">
        <field name="name">res.config.settings.assets.form</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Asset Classification Threshold" string="Asset Classification Threshold" name="Asset Classification Threshold">
                    <block title="Asset Classification Threshold">
                        <setting help="Asset Classification Threshold">
                            <field name="minor_major_threshold"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record> -->

    <record id="fixed_assets_config_settings_action" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <!-- Keep view selection dynamic to avoid hard failure when external IDs drift across databases. -->
        <field name="view_mode">form</field>
        <field name="context">{'module' : 'fixed_assets', 'bin_size': False}</field>
    </record>


</odoo>