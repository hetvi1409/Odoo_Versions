<odoo>
    <record id="view_project_issue_form" model="ir.ui.view">
        <field name="name">project.issue.form</field>
        <field name="model">project.issue</field>
        <field name="arch" type="xml">
            <form string="Project Issue">
                <sheet>
                    <style>
                        .o_form_view:not(.o_field_highlight)
                        .o_field_widget:not(.o_field_invalid):not(.o_field_highlight)
                        .o_input:not(:hover):not(:focus) {
                        --o-input-border-color: black !important;
                        }
                        .o_form_view:not(o_field_highlight)
                        .o_field_html .note-editable:not(:hover),
                        .o_form_view:not(o_field_highlight) .o_field_html
                        .note-editable:not(:focus) {
                        border-color: black !important;
                        }
                        .form-check:not(:hover),
                        .form-check:not(:hover)
                        .form-check-input:not(:disabled),
                        .form-select:not(:hover), .form-select:not(:hover)
                        .form-check-input:not(:disabled) {
                        border-color: black !important;
                        }
                    </style>
                    <group string="Issue Information">
                        <field name="name" readonly="1"/>
                        <field name="project_id" context="{'form_view_ref': 'project_porfolio_management.edit_project_ppe'}"
                             options="{'no_create': True}"/>
                        <field name="title"/>
                        <field name="description"/>
                        <field name="contact_person_id"/>
                    </group>
                    <group string="Issue Classification" colspan="2">
                        <group>
                            <field name="type"/>
                            <field name="status"/>
                            <field name="priority"/>
                        </group>
                    </group>
                    <group string="Issue Management" colspan="2">
                        <group>
                            <field name="issue_owner_id"/>
                            <field name="follow_up_date"/>
                            <field name="logged_date"/>
                        </group>
                        <group>
                            <field name="age" readonly="1"/>
                            <field name="days_overdue" readonly="1"/>
                            <field name="is_overdue" widget="badge" decoration-success="is_overdue == 'not_overdue'"
                                   decoration-warning="is_overdue == 'due_soon'" decoration-danger="is_overdue == 'overdue'"/>
                        </group>
                    </group>

                    <group string="Update Detail">
                        <field name="write_uid" widget="many2one_avatar" readonly="1"/>
                        <field name="write_date" readonly="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_project_issue_list" model="ir.ui.view">
        <field name="name">project.issue.list</field>
        <field name="model">project.issue</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="project_id"/>
                <field name="title"/>
                <field name="priority" widget="badge" decoration-success="priority == 'Low'"
                       decoration-warning="priority == 'Medium'" decoration-danger="priority == 'High'"/>
                <field name="status"/>
                <field name="issue_owner_id"/>
                <field name="follow_up_date"/>
                <field name="is_overdue" widget="badge" decoration-success="is_overdue == 'not_overdue'"
                       decoration-warning="is_overdue == 'due_soon'" decoration-danger="is_overdue == 'overdue'"/>
                <field name="write_date"/>
            </list>
        </field>
    </record>

    <record id="action_project_issue" model="ir.actions.act_window">
        <field name="name">Issues</field>
        <field name="res_model">project.issue</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_project_issue_root" name="Issues" parent="project_porfolio_management.menu_ppo_raid" sequence="3" action="action_project_issue"/>
</odoo>
