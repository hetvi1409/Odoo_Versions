<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_project_task_list_ppe" model="ir.ui.view">
        <field name="name">project.task.list.view</field>
        <field name="model">project.task</field>
        <field eval="50" name="priority"/>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="user_ids" widget="many2many_avatar_user"/>
                <field name="planned_date_begin" string="Planned Start Date"/>
                <field name="date_deadline" string="Planned End Date"/>
                <field name="baseline_start_date"/>
                <field name="baseline_end_date"/>
                <field name="variance_planned_start_date"/>
                <field name="variance_planned_end_date"/>
            </list>
        </field>
    </record>

    <record id="view_task_form_ppe" model="ir.ui.view">
        <field name="name">project.task.form</field>
        <field name="model">project.task</field>
        <field eval="50" name="priority"/>
        <field name="arch" type="xml">

            <form string="Task" class="o_form_project_tasks" js_class="project_task_form">
                <header>
                    <field name="stage_id" widget="statusbar_duration" options="{'clickable': '1', 'fold_field': 'fold'}" invisible="not project_id and not stage_id"/>
                    <field name="state" widget="statusbar" options="{'clickable': '1', 'fold_field': 'fold'}" invisible="1"/>
                    <field name="personal_stage_type_id" widget="statusbar" options="{'clickable': '1', 'fold_field': 'fold'}" invisible="project_id" domain="[('user_id', '=', uid)]" string="Personal Stage"/>
                </header>
                <!-- Used in inherited views to display the eventual warnings -->
                <t name="warning_section"/>
                <sheet string="Task">
                    <style>
                        .o_form_view:not(.o_field_highlight)
                        .o_field_widget:not(.o_field_invalid):not(.o_field_highlight)
                        .o_input:not(:hover):not(:focus) {
                        --o-input-border-color: black !important;
                        }
                        .o_form_view:not(o_field_highlight)
                        .o_field_html .note-editable:not(:hover),
                        .o_form_view:not(o_field_highlight) .o_field_html
                        .note-editable:not(:focus) {
                        border-color: black !important;
                        }
                        .form-check:not(:hover),
                        .form-check:not(:hover)
                        .form-check-input:not(:disabled),
                        .form-select:not(:hover), .form-select:not(:hover)
                        .form-check-input:not(:disabled) {
                        border-color: black !important;
                        }
                    </style>
                    <widget name="web_ribbon" title="Archived"
                            bg_color="text-bg-danger" invisible="active"/>
                    <group string="Task Information">
                        <group>
                            <field name="name"/>
                            <field name="description" options="{'height': 50}"/>
                        </group>
                        <group>
                            <field name="project_id"/>
                            <field name="code"/>
                            <field name="user_ids"
                                   widget="many2many_avatar_user"/>
                            <field name="milestone_id"/>
                        </group>
                    </group>
                    <group string="Planing Information">
                        <group>
                            <field name="date_deadline" required="True"
                                   widget="daterange"
                                   options="{'start_date_field': 'planned_date_begin', 'always_range': '1'}"
                                   string="Planing Date"/>
                            <field name="actual_end_date"/>
                            <label for="planned_progress" class="oe_inline"/>
                            <div>
                                <field name="planned_progress"
                                       widget="progressbar" class="oe_inline"/>
                            </div>
                            <label for="allocation" class="oe_inline"/>
                            <div>
                                <field name="allocation"
                                       class="oe_inline"/>
                                <field name="allocation"
                                       widget="progressbar" class="oe_inline"/>
                            </div>
                            <label for="variance" class="oe_inline"/>
                            <div>
                                <field name="variance"
                                       widget="progressbar" class="oe_inline"/>
                            </div>
                        </group>
                        <group>
                            <field name="baseline_end_date" widget="daterange"
                                   options="{'start_date_field': 'baseline_start_date', 'always_range': '1'}"
                                   string="Baseline Start\End Date"/>
                            <field name="variance_planned_start_date"/>
                            <field name="variance_planned_end_date"/>
                            <field name="type"/>
                            <field name="critical_path"/>
                            <field name="generate_planing"/>
                            <field name="sort_order"/>
                            <field name="days_overdue"/>
                        </group>
                    </group>
                </sheet>
                <chatter reload_on_follower="True"/>
            </form>
        </field>
    </record>

    <record id="action_view_all_task" model="ir.actions.act_window">
        <field name="name">Tasks</field>
        <field name="res_model">project.task</field>
        <field name="view_mode">list,kanban,form,calendar,pivot,graph,activity</field>
        <field name="view_ids" eval="[
            (5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_project_task_list_ppe')}),
            (0, 0, {'view_mode': 'kanban'}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_task_form_ppe')})
        ]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No tasks found. Let's create one!
            </p>
            <p>
                Organize your tasks by dispatching them across the pipeline.<br/>
                Collaborate efficiently by chatting in real-time or via email.
            </p>
        </field>
    </record>
    <menuitem id="action_view_all_task_menu" name="Tasks" parent="menu_ppo_time_and_cost"
              action="action_view_all_task"
              sequence="2"/>

</odoo>